# Zer0Fit — locked dependencies (installed AFTER the architecture-specific
# torch/torchvision wheel selected by the Dockerfile).
#
# NOTE: tabfm is NOT on PyPI. It is git-installed in the Dockerfile (and via
# install.sh for local non-container dev) with its [pytorch] extra and
# --no-deps so it never overwrites the carefully selected torch wheel.

mcp[cli]>=1.0.0
uvicorn>=0.30.1
starlette>=0.37.2
pandas>=2.2.0
scikit-learn>=1.4.0
accelerate>=0.30.0
huggingface_hub>=0.24.0
einops>=0.8.0
absl-py>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
# numpy is a transitive dep but pin explicitly — numpy 2.x has breaking changes
numpy>=1.26.0,<2.0.0
# timesfm is installed AFTER the arch-specific torch wheel. The [torch] extra
# declares torch as a dep — if pip's resolver tries to upgrade torch here,
# use --no-deps for this line in the Dockerfile or pin torch version.
timesfm[torch]==2.0.2